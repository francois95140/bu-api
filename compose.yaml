services:
  postgres:
    image: 'postgres:16.9'
    environment:
      - 'POSTGRES_DB=bibliotheque'
      - 'POSTGRES_USER=esgi'
      - 'POSTGRES_PASSWORD=esgi'
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: